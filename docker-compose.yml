services:
  backend:
    image: jhayhh/hh:datedeals
    container_name: datedeals
    # build:
    #   context: ./backend
    #  network: host
    env_file:
      - ./.env
    # depends_on:
    #   - db
    ports:
      - 8080:8080
    volumes:
      - ./logs:/home/logs

  # db:
  #   image: postgres:17.4-alpine
  #   #build:
  #   #  network: host
  #   container_name: datedeals-pg
  #   volumes:
  #     - ./db-data:/var/lib/postgresql/data:z
  #     - ./ssl:/etc/ssl
  #   env_file:
  #     - ./db/.env
  #   ports:
  #     - 5432:5432

  nginx:
    image: nginx:1.29.3-alpine
    container_name: datedeals-nginx
    depends_on:
      - backend
    ports:
      - 80:80
      - 443:443
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./ssl:/etc/nginx/ssl:ro
